#include <stdio.h>

int cube(int num)
{
    return num * num * num;
}

int main()
{
    int num, cube_num;

    printf("Enter a number: ");
    scanf("%d", &num);

    cube_num = cube(num);

    printf("The cube of %d is %d\n", num, cube_num);

    return 0;
}
